import 'package:flutter/material.dart';

class User extends StatelessWidget {

  final String username;
  final String mobileNo;
  User(this.username,this.mobileNo);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
